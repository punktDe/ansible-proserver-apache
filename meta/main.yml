---
galaxy_info:
  namespace: decix
  company: decix
  description: this roles prepares the punkt.de proserver to use apache as webserver
  license: open source
  min_ansible_version: 2.9.20
dependencies:
  - role: dehydrated
  - role: php_fpm
