---
apache:
  prefix:
    config: >-
      {%- if ansible_system == 'Linux' -%}
        /etc/apache2
      {%- else -%}
        /usr/local/etc/apache24
      {%- endif -%}
  user: "{{ 'www-data' if ansible_system == 'Linux' else 'www' }}"
  htpasswd: {}
